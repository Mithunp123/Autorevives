# Frontend Environment Configuration
# Copy this file to .env and update values for your environment

# API URL for backend server
# Development: Leave empty or use /api (uses Vite proxy)
# Production (Cloudflare): Set to your backend URL
# Example: VITE_API_URL=https://api.autorevives.com/api
VITE_API_URL=

# Note: After deploying frontend to Cloudflare, set this to your backend server URL
# The backend must have CORS configured to allow requests from your Cloudflare domain
